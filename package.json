{
  "name": "chess-pretraining",
  "private": true,
  "license": "MIT",
  "description": "Lint and build for the static frontend in web/",
  "type": "module",
  "scripts": {
    "vendor": "node scripts/vendor.mjs",
    "postinstall": "npm run vendor",
    "build": "node scripts/build-web.mjs",
    "lint": "eslint web"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "chessground": "9.2.1",
    "esbuild": "0.28.1",
    "eslint": "^10.8.0",
    "globals": "^17.8.0"
  }
}
