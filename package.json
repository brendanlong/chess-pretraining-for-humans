{
  "name": "chess-pretraining",
  "private": true,
  "description": "Lint config for the static frontend in web/ (no build step)",
  "type": "module",
  "scripts": {
    "lint": "eslint web"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "globals": "^17.7.0"
  }
}
